import type { SisterClassPageDef } from "../types";

/**
 * 1. Add `public/images/sisters/alpha-iota/class-composite.jpg` (or match `heroImageFile`).
 * 2. Add member portraits: `<#>-firstname-lastname.jpg` (see `../paths.ts`).
 * 3. Replace `null` with a `SisterClassPageDef` object and register stays in `registry.ts`.
 */
export const alphaIotaClassPage: SisterClassPageDef | null = null;
